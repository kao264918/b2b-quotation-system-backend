# B2B Quotation System 後端架構分析報告

生成日期：2026-01-20

---

## 一、架構概覽

這是一個基於 **FastAPI + SQLAlchemy 2.0 + PostgreSQL** 的 B2B 報價管理系統後端，採用典型的三層架構。

### 技術棧

- **Web 框架**：FastAPI
- **ORM**：SQLAlchemy 2.0
- **資料庫**：PostgreSQL
- **資料庫遷移**：Alembic
- **資料驗證**：Pydantic 2.x
- **ASGI 伺服器**：uvicorn

### 目錄結構

```
app/
├── models/      # 資料模型（13個模型）
├── schemas/     # Pydantic schemas（API 驗證）
├── crud/        # 資料庫操作層（15個）
├── routers/     # API 路由（13個）
├── services/    # 業務邏輯服務（9個）
├── config.py    # 配置管理
├── database.py  # 資料庫連接
└── main.py      # 應用程式入口
```

---

## 二、核心業務領域

系統主要處理以下業務模組：

1. **Customer Management**（客戶管理）
2. **Vendor Management**（廠商管理）
3. **Catalog Management**（產品目錄）
4. **RFQ System**（詢價單系統，核心功能）
5. **Quote Management**（報價單）
6. **Invoice Management**（發票）
7. **Template Management**（範本）
8. **Unit & Tax Settings**（單位與稅務設定）

---

## 三、架構設計亮點

### ✅ 1. 版本化 RFQ 系統（Versioned RFQ）

這是系統最複雜也最優秀的設計：

```
RFQ (主記錄)
├── RFQVersion (v1, v2, v3...) - 不可變快照
│   ├── vendor_snapshot (廠商資料快照)
│   ├── items (商品清單)
│   └── calculations (計算結果)
└── current_version_id / selected_version_id (指標)
```

**優點：**
- 完整的修改歷史追蹤
- 不可變快照設計（Immutable snapshots）
- 支援版本比較和回溯
- 避免資料覆蓋風險

### ✅ 2. 後端權威計算（Backend-Authoritative Calculations）

所有金額計算都在後端完成，前端不參與：
- 面積單位計算：`ceil((length * width) / 900)`
- 行項目小計：`unit_price × quantity × area_unit`
- 稅金計算：支援 5%、10%、免稅等多種模式
- 總計算法

### ✅ 3. CRUD 基類模式（Generic CRUD Base）

使用泛型實作 CRUD 基類，減少重複程式碼：
```python
class CRUDBase(Generic[ModelType, CreateSchemaType, UpdateSchemaType])
```

### ✅ 4. 清晰的狀態機設計

RFQ 有兩組獨立的狀態：
- **工作流狀態**：`DRAFT → VENDOR_QUOTING → FINALIZED → CLOSED/DISCARDED`
- **會計狀態**：`UNFULFILLED → FULFILLED → PAID`

### ✅ 5. 資料快照模式（Snapshot Pattern）

在建立 RFQVersion 時，會將廠商資料完整快照（JSONB），避免主資料變更影響歷史記錄。

---

## 四、存在的問題與風險

### 🔴 嚴重問題

#### 1. 缺乏認證與授權機制（Critical Security Issue）

**現況：**
```python
# 完全沒有身份驗證
@router.post("/")
def create_rfq(*, db: Session = Depends(get_db), rfq_in: RFQCreate):
    # 任何人都可以建立/修改/刪除資料
```

**風險：**
- 任何人都可以存取所有 API
- 沒有使用者權限控制
- 無法追蹤誰做了什麼操作
- 資料安全性為零

**建議修復：**
```python
from fastapi.security import OAuth2PasswordBearer
from jose import JWTError, jwt

oauth2_scheme = OAuth2PasswordBearer(tokenUrl="token")

async def get_current_user(token: str = Depends(oauth2_scheme)):
    # JWT 驗證邏輯
    pass

@router.post("/")
def create_rfq(
    *,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user),
    rfq_in: RFQCreate
):
    # 現在有身份驗證了
```

#### 2. 資料庫連線池未配置

**現況：**
```python
# database.py
engine = create_engine(settings.DATABASE_URL, echo=False)
# 沒有設定 pool_size, max_overflow, pool_timeout 等參數
```

**風險：**
- 高併發時連線耗盡
- 資料庫過載
- 請求超時

**建議配置：**
```python
engine = create_engine(
    settings.DATABASE_URL,
    echo=False,
    pool_size=20,           # 連線池大小
    max_overflow=10,        # 額外連線數
    pool_timeout=30,        # 等待超時
    pool_pre_ping=True,     # 連線健康檢查
    pool_recycle=3600       # 連線回收時間（1小時）
)
```

#### 3. CORS 配置過於寬鬆

**現況：**
```python
# main.py
allow_origins=[
    "http://localhost:5173",
    # ... 多個本地開發 URL
],
allow_origin_regex=r"https://.*\.vercel\.app",  # 允許所有 Vercel 子網域
allow_credentials=True,
allow_methods=["*"],     # 允許所有 HTTP 方法
allow_headers=["*"],     # 允許所有 Header
```

**風險：**
- CSRF 攻擊風險
- 任何 Vercel 子網域都能存取
- 過度開放的權限

**建議修正：**
```python
# 生產環境應該明確指定允許的來源
allow_origins=settings.CORS_ORIGINS,  # 從環境變數讀取
allow_methods=["GET", "POST", "PUT", "PATCH", "DELETE"],  # 明確指定
allow_headers=["Content-Type", "Authorization"],  # 限制 headers
```

---

### 🟠 重要問題

#### 4. 缺乏日誌系統

除了 `initial_data.py` 有基本的 logging，整個應用程式沒有結構化日誌。

**建議加入：**
```python
import logging
from logging.handlers import RotatingFileHandler

# 設定結構化日誌
logger = logging.getLogger("app")
logger.setLevel(logging.INFO)

# 檔案處理器（輪轉）
file_handler = RotatingFileHandler(
    "app.log",
    maxBytes=10*1024*1024,  # 10MB
    backupCount=5
)

# JSON 格式（便於分析）
formatter = logging.Formatter(
    '{"time":"%(asctime)s","level":"%(levelname)s","module":"%(module)s","message":"%(message)s"}'
)
file_handler.setFormatter(formatter)
logger.addHandler(file_handler)
```

#### 5. 錯誤處理不夠細緻

**現況：**
```python
try:
    rfq = rfq_crud.create_rfq(db, obj_in=rfq_in)
    return rfq
except ValueError as e:
    raise HTTPException(status_code=400, detail=str(e))
```

**問題：**
- 沒有統一的錯誤處理中介軟體
- 錯誤訊息可能洩漏敏感資訊
- 沒有錯誤追蹤（如 Sentry）
- 資料庫錯誤沒有正確處理

**建議改善：**
```python
# 統一的錯誤處理器
@app.exception_handler(Exception)
async def global_exception_handler(request: Request, exc: Exception):
    logger.error(f"Unhandled exception: {exc}", exc_info=True)
    return JSONResponse(
        status_code=500,
        content={"detail": "Internal server error", "request_id": str(uuid.uuid4())}
    )

# 自訂業務例外
class BusinessException(Exception):
    def __init__(self, message: str, error_code: str):
        self.message = message
        self.error_code = error_code
```

#### 6. 沒有請求速率限制（Rate Limiting）

任何人都可以無限制地發送請求，可能導致 DDoS 攻擊。

**建議使用 slowapi：**
```python
from slowapi import Limiter, _rate_limit_exceeded_handler
from slowapi.util import get_remote_address

limiter = Limiter(key_func=get_remote_address)
app.state.limiter = limiter
app.add_exception_handler(RateLimitExceeded, _rate_limit_exceeded_handler)

@router.post("/")
@limiter.limit("10/minute")  # 每分鐘最多 10 次
def create_rfq(...):
    pass
```

#### 7. 缺乏輸入驗證與過濾

雖然有 Pydantic schema，但沒有額外的安全驗證：
- SQL Injection 防護（SQLAlchemy 有基本防護，但應加強）
- XSS 防護（HTML 標籤過濾）
- 檔案上傳驗證（如果有的話）

#### 8. 測試覆蓋率極低

```bash
# 只有少數測試檔案
tests/test_rfq_snapshot_service.py
test_catalog_api.py
```

**建議達到：**
- 單元測試覆蓋率 > 80%
- 整合測試覆蓋核心業務流程
- API 端點測試

---

### 🟡 次要問題

#### 9. 環境變數管理不完善

**現況：**
```python
# .env.example 只有兩個變數
DATABASE_URL=...
CORS_ORIGINS=...
```

**缺少：**
- 環境區分（dev/staging/prod）
- 密鑰管理（JWT_SECRET_KEY）
- 第三方服務配置
- 日誌級別配置

#### 10. 資料庫索引不完整

**現況：**
```python
# models/rfq.py
rfq_no: Mapped[str] = mapped_column(String(20), unique=True, nullable=False, index=True)
```

**應該加入更多索引：**
```python
# 常用查詢欄位應該加索引
status: Mapped[str] = mapped_column(String(20), index=True)
created_at: Mapped[datetime] = mapped_column(DateTime, index=True)

# 複合索引（多欄位查詢）
__table_args__ = (
    Index('idx_rfq_status_created', 'status', 'created_at'),
)
```

#### 11. 缺乏健康檢查端點細節

**現況：**
```python
@app.get("/health")
def health_check():
    return {"status": "healthy"}
```

**應該檢查：**
- 資料庫連線狀態
- 第三方服務狀態
- 記憶體使用率

```python
@app.get("/health")
async def health_check(db: Session = Depends(get_db)):
    try:
        # 檢查資料庫連線
        db.execute(text("SELECT 1"))
        return {"status": "healthy", "database": "ok"}
    except Exception as e:
        return JSONResponse(
            status_code=503,
            content={"status": "unhealthy", "database": "error"}
        )
```

#### 12. 缺乏詳細的 API 文件

FastAPI 的自動文件很好，但很多 endpoint 缺少詳細說明：

**現況：**
```python
@router.post("/")  # 沒有 description, summary
def create_rfq(...):
    pass
```

**應該改成：**
```python
@router.post(
    "/",
    summary="建立新的詢價單",
    description="建立一個新的 RFQ 並自動產生版本 v1",
    response_description="成功建立的 RFQ 物件"
)
def create_rfq(...):
    """
    建立新的詢價單 (RFQ)

    - **vendor_id**: 廠商 ID（必填）
    - **project_name**: 專案名稱（必填）
    - **items**: 商品清單
    """
    pass
```

#### 13. 沒有資料備份策略

Dockerfile 沒有包含資料備份機制，應該在部署文件中說明：
- 定期資料庫備份
- 備份保留策略
- 災難恢復計畫

#### 14. 缺乏效能監控

沒有 APM（Application Performance Monitoring）工具整合：
- 建議整合 New Relic / Datadog / Sentry
- 慢查詢追蹤
- API 效能分析
- 記憶體使用監控

#### 15. 資料庫遷移命名不一致

**現況：**
```
alembic/versions/
├── 001_initial.py
├── 002_catalog_mvp.py
├── 0d11176827a9_add_rfq_deleted_at_for_soft_delete.py  # 混雜格式
├── 27c8ca4dcd57_fix_customer_schema_mismatch.py
```

**建議：**
- 統一使用 Alembic 自動產生的檔名
- 或統一使用數字前綴命名

#### 16. Customer 和 Vendor 模型混用

**現況：**
```python
# RFQ 模型中
vendor_id: Mapped[str] = mapped_column(ForeignKey("customers.id"))
# 註解：Using Customer table with role='vendor'
```

**問題：**
這種設計可能造成混淆，應該：
- 在資料庫層面統一為 `customers` 表（用 role 區分）
- 在應用層保持語義清晰（用 alias 或 view）
- 或者完全分離為兩張表

---

## 五、安全性檢查清單

| 項目 | 狀態 | 優先級 |
|------|------|--------|
| 身份驗證（JWT/OAuth） | ❌ 缺少 | 🔴 緊急 |
| 授權與權限控制（RBAC） | ❌ 缺少 | 🔴 緊急 |
| HTTPS 強制 | ⚠️ 未知 | 🔴 緊急 |
| SQL Injection 防護 | ✅ ORM 提供 | 🟢 良好 |
| XSS 防護 | ⚠️ 部分 | 🟠 重要 |
| CSRF 防護 | ❌ 缺少 | 🟠 重要 |
| 敏感資料加密 | ❌ 缺少 | 🟠 重要 |
| 請求速率限制 | ❌ 缺少 | 🟠 重要 |
| 輸入驗證 | ✅ Pydantic | 🟢 良好 |
| 錯誤訊息過濾 | ⚠️ 部分 | 🟡 次要 |
| 日誌記錄 | ❌ 缺少 | 🟠 重要 |
| 監控告警 | ❌ 缺少 | 🟡 次要 |

---

## 六、效能優化建議

### 1. 資料庫查詢優化

**現況：**
部分地方已使用 `joinedload` / `selectinload` 避免 N+1 問題。

**建議：**
- 檢查所有查詢，確保沒有 N+1 問題
- 加入更多資料庫索引
- 考慮使用 Redis 快取熱門資料（如產品目錄）

### 2. 分頁與限制

**現況：**
已有分頁實作（✅）
```python
page_size: int = Query(50, ge=1, le=150)
```

**建議：**
- 預設最大限制 150 可能太高，建議降到 100
- 考慮加入 cursor-based pagination（更適合大數據集）

### 3. 異步處理

**現況：**
FastAPI 使用同步函式。

**建議：**
- 考慮將關鍵路徑改用 `async/await`
- 長時間任務使用背景任務（Celery/RQ/FastAPI BackgroundTasks）
- 如 PDF 匯出、大量資料處理等

### 4. 資料庫連線管理

**建議：**
- 使用連線池（已建議在問題 #2）
- 監控連線池使用率
- 設定合理的超時時間

---

## 七、部署與維運建議

### 現有配置

| 項目 | 狀態 | 說明 |
|------|------|------|
| 容器化 | ✅ 已實作 | 有 Dockerfile |
| 環境變數 | ✅ 已實作 | 使用 .env |
| 資料庫遷移 | ✅ 已實作 | 使用 Alembic |
| 健康檢查 | ⚠️ 基礎 | 需加強檢查項目 |
| 日誌收集 | ❌ 缺少 | 需實作 |
| 監控告警 | ❌ 缺少 | 需實作 |
| 備份策略 | ❌ 未提及 | 需規劃 |

### 建議的 DevOps 實踐

#### 1. CI/CD Pipeline
```yaml
# .github/workflows/deploy.yml 範例
name: Deploy
on:
  push:
    branches: [main]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run tests
        run: pytest
  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to production
        run: # 部署腳本
```

#### 2. 環境分離
- **Development**：本地開發環境
- **Staging**：測試環境（mirror production）
- **Production**：生產環境

#### 3. 監控指標
- API 回應時間
- 錯誤率
- 資料庫查詢效能
- CPU/記憶體使用率
- 請求 QPS

#### 4. 日誌策略
- **應用日誌**：結構化 JSON 格式
- **存取日誌**：Nginx/uvicorn access log
- **錯誤日誌**：整合 Sentry
- **審計日誌**：記錄重要操作（建立/修改/刪除 RFQ）

#### 5. 備份策略
- **資料庫**：每日全量備份 + 持續 WAL 歸檔
- **保留期**：30 天
- **備份測試**：每月測試恢復流程
- **異地備份**：使用不同雲端區域

---

## 八、建議的改善優先級

### 🔴 第一優先（1-2 週）

1. **實作身份驗證與授權系統**
   - 整合 JWT 認證
   - 實作 RBAC 權限控制
   - 加入使用者管理 API

2. **設定資料庫連線池**
   - 配置 pool_size, max_overflow
   - 加入連線健康檢查
   - 監控連線池使用率

3. **修正 CORS 配置**
   - 限制允許的來源
   - 明確指定 HTTP 方法
   - 生產環境收緊權限

4. **加入結構化日誌**
   - 實作 JSON 格式日誌
   - 加入請求追蹤 ID
   - 整合日誌收集系統

5. **實作請求速率限制**
   - 使用 slowapi 或 Redis
   - 設定合理的限制閾值
   - 加入 IP 白名單機制

---

### 🟠 第二優先（2-4 週）

6. **完善錯誤處理機制**
   - 統一的錯誤處理中介軟體
   - 自訂業務例外類別
   - 整合 Sentry 錯誤追蹤

7. **加入 XSS/CSRF 防護**
   - HTML 標籤過濾
   - CSRF token 驗證
   - Content Security Policy

8. **提升測試覆蓋率**
   - 單元測試覆蓋率 > 80%
   - 整合測試核心流程
   - API 端點測試

9. **加入資料庫索引**
   - 分析慢查詢
   - 在常用欄位加索引
   - 建立複合索引

10. **整合效能監控工具**
    - APM 工具（New Relic/Datadog）
    - 慢查詢追蹤
    - 記憶體分析

---

### 🟡 第三優先（1-2 個月）

11. **改善 API 文件**
    - 加入詳細的 endpoint 說明
    - 提供範例請求/回應
    - 錯誤碼說明文件

12. **實作資料備份策略**
    - 自動化備份腳本
    - 備份監控告警
    - 定期恢復測試

13. **重構 Customer/Vendor 模型**
    - 統一資料模型
    - 改善語義清晰度
    - 資料遷移腳本

14. **優化資料庫查詢效能**
    - 使用 explain analyze 分析
    - 消除所有 N+1 查詢
    - 加入查詢快取

15. **加入快取層（Redis）**
    - 快取熱門資料
    - Session 管理
    - 分散式鎖

---

## 九、總結

### 優點 ✅

- **清晰的分層架構**：Models → CRUD → Services → Routers
- **版本化 RFQ 設計優秀**：完整的歷史追蹤與回溯能力
- **後端權威計算**：確保資料一致性與正確性
- **現代化技術棧**：FastAPI + SQLAlchemy 2.0 + Pydantic 2.x
- **資料庫遷移機制**：使用 Alembic 管理 schema 變更
- **狀態機設計清晰**：工作流狀態與會計狀態分離

### 核心問題 ❌

1. **缺乏身份驗證與授權**（最嚴重）
2. **安全性配置不足**（CORS、CSRF、Rate Limiting）
3. **缺乏完整的錯誤處理與日誌**
4. **測試覆蓋率太低**
5. **缺乏監控與告警機制**
6. **資料庫連線池未配置**

### 整體評估

這是一個結構良好的 **MVP 階段**後端系統，業務邏輯設計優秀，特別是版本化 RFQ 的實作展現了良好的軟體工程實踐。然而，**安全性和維運成熟度明顯不足**。

### 上線建議

**⚠️ 不建議直接上線生產環境**

建議至少完成以下項目後再考慮正式部署：

1. ✅ 實作身份驗證與授權
2. ✅ 設定資料庫連線池
3. ✅ 修正 CORS 配置
4. ✅ 加入結構化日誌
5. ✅ 實作請求速率限制
6. ✅ 完善錯誤處理
7. ✅ 提升測試覆蓋率到 60% 以上

完成這些項目後，系統將具備基本的生產環境部署條件。

---

## 附錄

### A. 推薦的技術選型

| 用途 | 推薦工具 | 說明 |
|------|---------|------|
| 身份驗證 | python-jose | JWT token 處理 |
| 密碼加密 | passlib + bcrypt | 安全的密碼雜湊 |
| 速率限制 | slowapi | FastAPI 整合的限流工具 |
| 錯誤追蹤 | Sentry | 生產環境錯誤監控 |
| 效能監控 | Datadog / New Relic | APM 工具 |
| 快取 | Redis | 分散式快取 |
| 任務佇列 | Celery + Redis | 背景任務處理 |
| 日誌收集 | ELK / Loki | 集中式日誌管理 |

### B. 環境變數範例

```bash
# 應用程式設定
APP_NAME=B2B Quotation System
APP_ENV=production
DEBUG=false

# 資料庫
DATABASE_URL=postgresql://user:pass@host:5432/db
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=10

# 安全性
SECRET_KEY=your-secret-key-here
JWT_SECRET_KEY=your-jwt-secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS
CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# Redis
REDIS_URL=redis://localhost:6379/0

# 監控
SENTRY_DSN=https://xxx@sentry.io/xxx

# 日誌
LOG_LEVEL=INFO
LOG_FORMAT=json
```

### C. 參考資源

- [FastAPI Security](https://fastapi.tiangolo.com/tutorial/security/)
- [SQLAlchemy 2.0 Documentation](https://docs.sqlalchemy.org/)
- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [12-Factor App](https://12factor.net/)

---

**報告結束**
